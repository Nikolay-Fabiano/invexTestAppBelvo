<div class="card">
  <div class="card-body">
    <%= link_to 'Home', root_path, class: 'btn btn-primary' %>
  </div>
</div>

<div class="text-center">
  <%= render 'currencies' %>
  <%= render 'accounts' %>
  <%= render 'transactions' %>
</div>
